import React from "react";

/**
 * Header Component
 *
 * This component represents the header of the application, displaying the title.
 *
 * @returns {JSX.Element} The Header component JSX.
 */
const Header = () => {
  return (
    <header role="header">
      <h1 className="title">HRnet</h1>
    </header>
  );
};

export default Header;
